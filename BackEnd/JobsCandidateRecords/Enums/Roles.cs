namespace JobsCandidateRecords.Enums
{
    /// <summary>
    /// Defines the different roles that a user can have in the system.
    /// </summary>
    public enum Roles
    {
        /// <summary>
        /// Represents a standard user with basic access rights.
        /// </summary>
        User,

        /// <summary>
        /// Represents an administrator with elevated access rights and control over the system.
        /// </summary>
        Admin
    }
}
